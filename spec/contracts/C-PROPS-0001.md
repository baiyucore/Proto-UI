# Props records must remain serializable

Props are part of the adapter contract surface. Keeping them serializable makes adapter output easier to inspect, compare, snapshot, and replay across host runtimes.
