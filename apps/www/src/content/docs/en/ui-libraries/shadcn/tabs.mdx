---
title: 'Tabs'
description: 'Shadcn tabs reconstructed on Proto UI.'
---

import AdapterPanelScript from '@/components/PrototypePreviewer/AdapterPanelScript.astro';
import PrototypePreviewer from '@/components/PrototypePreviewer/PrototypePreviewer.astro';
import { codeMap } from '@/content/docs/demo_components/tabs/tabsCode';
import { getCode } from '@/utils/conversionUtils';

<div data-adapter-panel="wc">
  <PrototypePreviewer
    demoId="demo-shadcn-tabs"
    initialRuntime="wc"
    toolbar={false}
    runtimes={['wc']}
    hasCode={true}
    code={getCode('wc', 'demo-shadcn-tabs', codeMap)}
  />
</div>

<div data-adapter-panel="react" style="display:none;">
  <PrototypePreviewer
    demoId="demo-shadcn-tabs"
    initialRuntime="react"
    toolbar={false}
    runtimes={['react']}
    hasCode={true}
    code={getCode('react', 'demo-shadcn-tabs', codeMap)}
  />
</div>

<div data-adapter-panel="vue" style="display:none;">
  <PrototypePreviewer
    demoId="demo-shadcn-tabs"
    initialRuntime="vue"
    toolbar={false}
    runtimes={['vue']}
    hasCode={true}
    code={getCode('vue', 'demo-shadcn-tabs', codeMap)}
  />
</div>

<div data-adapter-panel="vue2" style="display:none;">
  <PrototypePreviewer
    demoId="demo-shadcn-tabs"
    initialRuntime="vue2"
    toolbar={false}
    runtimes={['vue2']}
    hasCode={true}
    code={getCode('vue2', 'demo-shadcn-tabs', codeMap)}
  />
</div>

<AdapterPanelScript />

## Content mount policy

`TabsContent` uses a lazy L1 view by default:

- only the current panel materializes a host view;
- an inactive panel detaches its view while preserving its Proto instance, state, and exposes;
- `keepMounted={true}` is the only view-retention option and keeps the complete inactive view mounted.

Web Component keeps the custom element as the owner and hides that owner while its internal view is detached. Reactivating the panel materializes a new view inside the same Proto instance owner.
