---
title: 'Props'
desp: 'Props 规范'
description: 'Props 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-PROPS-0001',
    title: 'Props 是 Maker 到原型的配置通路',
    summary:
      'Props 承载应用作者传入原型的配置数据，是 Proto UI 当前 portable channel model 中明确接纳的 Maker-to-component 通路。',
    quote: 'Props carries configuration data from the app maker into a prototype.',
    criteria: ['它 refinement 到 Core channel 契约，而不是某个宿主框架的 props 习惯。'],
    tests: '作为上层身份契约，被后续声明、解析、watcher 与 runtime 集成测试共同覆盖。',
    source: 'spec/contracts/C-PROPS-0001.yaml',
  },
  {
    id: 'C-PROPS-0002',
    title: 'Props 区分 setup 声明面与 runtime 读取面',
    summary: '声明、默认值规划和 watcher 注册发生在 `def.props`；运行时读取发生在 `run.props`。',
    quote:
      '`def.props.*` APIs must be setup-only; `run.props.*` APIs must not expose declaration, default-planning, or watcher-registration capabilities.',
    criteria: [
      '`def.props.define` / `setDefaults` / `watch*` 是 setup-only。',
      '`run.props.get` / `getRaw` / `isProvided` 是 runtime read surface。',
    ],
    tests: 'T-PROPS-0007、T-PROPS-0008 与 runtime run-props wiring 测试覆盖分期边界。',
    source: 'spec/contracts/C-PROPS-0002.yaml',
  },
  {
    id: 'C-PROPS-0003',
    title: 'Props 值必须保持 JSON value 边界',
    summary: '可移植 props 只能使用 JSON 可表达的数据，以保证跨 host 的序列化与语义一致。',
    quote:
      'Props values must be JSON values so that the props channel remains serializable, portable, and semantically consistent across hosts.',
    criteria: [
      '允许 string、number、boolean、null、array、object。',
      '不允许 undefined、function、symbol、bigint、Date、Map、Set、DOM object 等非 JSON 数据进入 portable props 语义。',
    ],
    tests: 'T-PROPS-0001 覆盖 JSON primitive、container、nested non-JSON 与 raw escape hatch。',
    source: 'spec/contracts/C-PROPS-0003.yaml',
  },
  {
    id: 'C-PROPS-0004',
    title: '每个 prop key 独立分类语义状态',
    summary: '`missing`、`provided-empty`、`provided-non-empty`、`invalid` 都按 key 独立判定。',
    quote:
      'The `missing`, `provided-empty`, or `invalid` state of one key must not change the classification or resolution semantics of another key.',
    criteria: [
      '缺失看 own property。',
      '`undefined` 在进入 resolved 语义前按 empty input 处理。',
      '某个 key 非法不得让整份 props record 失效。',
    ],
    tests:
      'T-PROPS-0005 与 T-PROPS-0006 通过 snapshot shape 和 fallback resolution 覆盖 key granularity。',
    source: 'spec/contracts/C-PROPS-0004.yaml',
  },
  {
    id: 'C-PROPS-0005',
    title: 'Setup 期声明是可合并的计划',
    summary: '多次 `define` 贡献同一份可配置 prop surface，最终在 runtime 执行前形成 merged plan。',
    quote:
      '`Setup`-time props `define` calls contribute to a planned configurable prop surface and can be merged before `runtime` execution.',
    criteria: [
      '声明不是 runtime 写入。',
      '同一 key 的多次声明必须进入 merge 规则，而不是后写直接覆盖。',
    ],
    tests: 'T-PROPS-0004 的 declaration merge 测试覆盖声明合并路径。',
    source: 'spec/contracts/C-PROPS-0005.yaml',
  },
  {
    id: 'C-PROPS-0006',
    title: 'Prop 声明 descriptor 有受限形状',
    summary:
      'v0 descriptor 至少约束 `type`、`empty`、enum `options`、`range` 与 JSON-valued `default`。',
    quote:
      'Descriptor shape validation failures must produce blocking errors, not warnings or silent ignores.',
    criteria: [
      '`type` 必须是 `any`、`boolean`、`string`、`number`、`object` 或 `enum`。',
      '`enum` 必须有非空 string `options`。',
      '`default` 必须满足 JSON props value boundary。',
    ],
    tests: 'T-PROPS-0001 与 T-PROPS-0004 覆盖 default 边界和 descriptor merge 形状。',
    source: 'spec/contracts/C-PROPS-0006.yaml',
  },
  {
    id: 'C-PROPS-0007',
    title: '声明合并要保持演进安全',
    summary: '合并必须拒绝破坏性收窄，并将放宽行为保留为可追踪 warning。',
    quote:
      'Prop declaration merging must reject breaking narrowing and keep widening changes traceable.',
    criteria: [
      'same-key merge 的 `type` 必须一致。',
      '`empty` 变严格是 error，变宽松是 warning 且保留 base。',
      'enum options 与 range 收窄是 error，放宽是 warning。',
    ],
    tests:
      'T-PROPS-0004 覆盖 type、empty、enum、range、validator、default 与 deterministic merge。',
    source: 'spec/contracts/C-PROPS-0007.yaml',
  },
  {
    id: 'C-PROPS-0008',
    title: 'Runtime resolution 产出 declared-key resolved snapshot',
    summary:
      '`resolved props` 包含且仅包含声明过的 key，不暴露 `undefined`，并以 `null` 作为规范空值。',
    quote: '`resolved snapshot` 必须包含每一个已声明的 `prop key`。',
    criteria: [
      '`run.props.get()` 返回当前 runtime invocation 对应的 resolved snapshot。',
      '未声明 raw key 不进入 resolved snapshot。',
      'resolved snapshot 在语义上只读。',
    ],
    tests:
      'T-PROPS-0005 覆盖 current snapshot、declared keys、undeclared exclusion、null normalization 与 readonly 语义。',
    source: 'spec/contracts/C-PROPS-0008.yaml',
  },
  {
    id: 'C-PROPS-0009',
    title: 'Empty 与 invalid 通过确定性 fallback 解析',
    summary:
      '`missing`、`provided-empty` 与 `invalid` 经过 `empty` 行为和 fallback chain 得到稳定 resolved value。',
    quote:
      'The fallback candidate order must be `prevValid`, `setDefaults`, declaration `default`, then final `null`.',
    criteria: [
      '`empty: "accept"` 将 provided-empty 解析为 `null`。',
      '默认 `empty: "fallback"` 进入 fallback chain。',
      '`empty: "error"` 要求 fallback 结果是非空有效值。',
    ],
    tests:
      'T-PROPS-0006 覆盖 missing、empty accept、empty fallback、invalid fallback、fallback order、null fallback 与 prevValid。',
    source: 'spec/contracts/C-PROPS-0009.yaml',
  },
  {
    id: 'C-PROPS-0010',
    title: '失败的声明合并不得部分应用',
    summary:
      'setup 期合并遇到 blocking diagnostics 时，整批 incoming declaration 作为一个 transaction 被拒绝。',
    quote:
      'Any blocking merge error must prevent the entire incoming declaration batch from applying.',
    criteria: [
      '失败后已有声明的 observable behavior 不变。',
      '失败尝试中产生的 partial warnings 不应被记录。',
    ],
    tests: 'T-PROPS-0004 覆盖 atomic batch rejection、behavior unchanged 与 no partial warnings。',
    source: 'spec/contracts/C-PROPS-0010.yaml',
  },
  {
    id: 'C-PROPS-0011',
    title: 'Resolved watcher 观察 resolved snapshot diff',
    summary:
      'resolved watcher 基于 declared-key resolved snapshot diff 触发，跳过 hydration，并保持 setup 注册顺序。',
    quote:
      'Resolved watcher triggering must be based on declared-key `resolved snapshot` diffs, not on whether raw props were provided again.',
    criteria: [
      'diff 使用 `Object.is`。',
      '`watchAll` 匹配所有 changed declared keys。',
      '`watch(keys)` 只在 watched key 变化时触发。',
      '多个 raw apply 可 coalesce 成一个 dispatch window。',
    ],
    tests:
      'T-PROPS-0007 覆盖 registration、hydration skip、Object.is、watchAll、watch(keys)、callback snapshots、change info、coalescing 与 dispatch order。',
    source: 'spec/contracts/C-PROPS-0011.yaml',
  },
  {
    id: 'C-PROPS-0012',
    title: 'Raw props API 是 escape hatch',
    summary:
      '`raw props` 暴露 adapter 提交给 Props module 的 pre-resolution snapshot，但不扩展 portable props 语义。',
    quote:
      'Raw props APIs expose the adapter-supplied pre-resolution props snapshot as an escape hatch without extending portable props semantics.',
    criteria: [
      'raw snapshot 可包含未声明 key。',
      'raw watcher 基于 raw union-key diff。',
      'raw watchers 先于 resolved watchers dispatch。',
    ],
    tests:
      'T-PROPS-0008 覆盖 raw definition、getRaw、undeclared keys、portability boundary、watchRaw 与 dispatch order。',
    source: 'spec/contracts/C-PROPS-0012.yaml',
  },
  {
    id: 'C-PROPS-0013',
    title: 'Props watcher callback 遵守 runtime run handle 绑定',
    summary:
      'watcher callback 中的 `run.props.*` 必须与当前 dispatch window 的 next snapshot 对齐。',
    quote:
      "Inside a resolved props watcher callback, `run.props.get()` must align with that callback's `next` resolved snapshot.",
    criteria: [
      'resolved watcher 与 raw watcher 都接收当前 callback invocation 的 run handle。',
      '`isProvided(key)` 使用当前 raw snapshot 的 own-property 语义。',
    ],
    tests:
      'runtime run-props-wiring 与 props-integration 测试覆盖 callback-time run handle alignment。',
    source: 'spec/contracts/C-PROPS-0013.yaml',
  },
  {
    id: 'C-PROPS-0014',
    title: 'applyRawProps 更新 props channel 但不隐式 render commit',
    summary:
      'controller 应用 raw props 会走 resolution 与 watcher dispatch 语义，但这个动作本身不代表渲染提交。',
    quote: '`applyRawProps` itself must not implicitly trigger a render commit.',
    criteria: [
      '`applyRawProps` 是 runtime/controller action，不是原型作者 API。',
      '它可以触发 raw 或 resolved watcher。',
      'render commit 的正向调度属于更上游 core runtime/update 契约。',
    ],
    tests:
      'T-PROPS-0011 及 runtime integration 覆盖 applyRawProps 更新、watcher dispatch 与 no implicit commit 的边界。',
    source: 'spec/contracts/C-PROPS-0014.yaml',
  },
];

## Props 是什么？

`Props` 是 Proto UI 的信息通路之一，承载 App Maker 流向 Component 的配置输入。它让外部调用者能够告诉组件：这一次调用应该使用哪些参数。

如果没有 Props 通路，App Maker 几乎无法主动向组件传输数据。组件仍然可以拥有内部状态、响应用户事件、向外暴露能力，但外部调用者无法稳定地配置它。

这条通路不是 React props、Vue props 或 Web Component attributes 的简单统一命名。不同宿主可以用不同方式接收输入，但进入 Proto UI 之后，Props 需要遵守同一套声明、解析、读取和监听语义。

## Props 的执行时期

Props API 按 `setup` 和 `runtime` 两个时期划分。

`setup` 期用于描述组件的可配置面：定义 props 的形状、设置默认值、注册监听器。此时真实调用尚未发生，所以不能读取本次调用传入的 props。

`runtime` 期用于读取本次调用的 props 值。此时组件已经进入实际运行，不能再定义新的 props 形状，也不能新增 watcher。Proto UI 禁止 runtime 期引入这类结构性副作用。

可以把 Props 理解成这条链路：

```txt
raw props
  -> declared prop shape
  -> resolution
  -> resolved props
  -> runtime read / watcher callback
```

## 定义 Props

<SpecPhaseBadge label="setup-only" />

`def.props.define()` 用于声明组件接受哪些 props，以及每个 key 的值应该如何被解释。它定义的是组件的可配置面，不是给组件写入一次运行时数据。

```ts
type TooltipProps = {
  disabled: boolean;
  side: 'top' | 'right' | 'bottom' | 'left';
  delay: number;
};

def.props.define<TooltipProps>({
  disabled: { type: 'boolean', default: false },
  side: {
    type: 'enum',
    options: ['top', 'right', 'bottom', 'left'],
    default: 'bottom',
  },
  delay: { type: 'number', range: { min: 0, max: 1000 }, default: 100 },
});
```

同一个原型可以多次调用 `define()`。这些声明会被合并，而不是简单地以后一次覆盖前一次。可兼容的声明可以共同形成最终 props shape；不可兼容的声明会被拒绝并抛错。具体合并规则可查 `C-PROPS-0007`。

Props value 只支持 JSON value。`string`、`number`、`boolean`、`null`、array 和 plain object 可以成为 portable props value；函数、日期、正则表达式、DOM object、class instance 等不能成为 portable props value。`default` 也必须满足同样的 JSON value 边界。

## 监听 Props 变化

<SpecPhaseBadge label="setup-only" />

`def.props.watch()` 用于注册 props 变化监听器。它观察的是 resolved props 的变化，而不是每一次 raw props 传入动作。

```ts
def.props.watch(['disabled'], (run, next, prev, info) => {
  if (next.disabled !== prev.disabled) {
    run.update();
  }
});
```

重新传入 props 不会自动导致 UI 更新。它只会更新 Props 通路中的数据，并在相关值真的发生变化时触发 watcher。是否发生 render commit 属于更上层 runtime/update 语义，不由 Props 通路隐式决定。

watcher 的触发按 key 独立判断。连续两次传入相同的 props，不应该触发 callback。key `a` 发生变化，不会触发只监听 key `b` 的 callback。一个 watcher 可以同时监听多个 key，只要其中任意一个被监听的 key 发生变化，这个 watcher 就会触发。

`def.props.watchAll()` 可以监听所有 declared props 的 resolved value 变化。初始 hydration 不触发 watcher；watcher 只响应后续变化。

## 获取 Props 的值

<SpecPhaseBadge label="runtime-only" />

`run.props.get()` 返回当前 runtime invocation 的 resolved props snapshot。resolved props 是经过声明和 resolution 处理后的结果：

- 只包含声明过的 key。
- 未声明但传入的 key 会被丢弃。
- 不暴露 `undefined`。
- 空结果使用 `null`。
- 非法值会根据该 key 的 `empty` / fallback 规则处理。

```ts
const props = run.props.get();

if (!props.disabled) {
  // 根据本次调用的 resolved props 执行逻辑。
}
```

`run.props.isProvided(key)` 用来判断某个 key 是否在当前 raw props 中被显式提供过。它回答的是“调用者有没有传这个 key”，不是“resolved value 是否非空”。

raw props 和 resolved props 都应该被视为不可修改的数据快照。修改这些对象没有协议意义，也不应该被用来传递状态变化。

## 用于诊断的 Raw Props API

`run.props.getRaw()`、`def.props.watchRaw()` 和 `def.props.watchRawAll()` 属于 raw props escape hatch。它们暴露的是 adapter 提交给 Props module 的 pre-resolution snapshot，适合诊断、调试、兼容宿主差异，或在极少数情况下作为逃生舱使用。

raw props 不等于宿主原始输入的绝对全量。React、Vue、Web Component 对 props、attrs、listeners 和 DOM attributes 的分流方式并不相同，adapter 可以决定哪些宿主输入会进入 raw props。

Raw API 不扩展 portable props 语义。raw props 可以包含未声明 key，也可能包含宿主相关值；依赖这些值会削弱跨平台兼容性。正式原型逻辑应优先依赖 resolved props。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Props 的测试映射主要落在 `spec/tests/T-PROPS-0001..0011.yaml`，并由 module、runtime 与 adapter conformance 测试共同覆盖。

| 测试实体       | 主要覆盖                                           |
| -------------- | -------------------------------------------------- |
| `T-PROPS-0001` | JSON props value boundary                          |
| `T-PROPS-0004` | declaration merge safety 与 failed merge atomicity |
| `T-PROPS-0005` | resolved snapshot shape                            |
| `T-PROPS-0006` | empty / invalid fallback resolution                |
| `T-PROPS-0007` | resolved watcher diff、coalescing、dispatch order  |
| `T-PROPS-0008` | raw props escape hatch 与 raw watcher              |
| `T-PROPS-0011` | `applyRawProps` runtime/controller integration     |

这些测试不只是实现回归测试，也是在把 YAML 契约实体变成可执行的行为边界。

## 与其他规范的关系

- `Core` 定义 channel、value 与执行时期等共通基础；Props 是其中一条具体通路。
- `State` 表达组件在时间中的内部连续性；Props 表达外部配置输入。
- `Event` 表达 user-to-component 的交互输入；Props 表达 maker-to-component 的配置输入。
- `Context` 更适合组件间共享环境；Props 更适合显式、局部、由使用者传入的配置。
- runtime/update 仍需承接 `run.update()` 与 render commit 的正向调度规则；`C-PROPS-0014` 只明确 props update 本身不隐含 commit。
