---
title: 'Event'
desp: 'Event 规范'
description: 'Event 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-EVENT-0001',
    title: 'Event 是 User 到 Component 的信息通路',
    summary: 'Event 承载 User 对组件发起的操作、输入与交互意图。',
    quote: 'Event is the User → Component information channel in Proto UI.',
    criteria: [
      'Event 必须被识别为 User → Component 信息通路。',
      'Event 必须承载 User 对组件发起的操作、输入或交互意图。',
      'Event callback 是组件响应 User 输入的 runtime 入口。',
      '没有 Event 时，组件无法得知何时发生了 User 交互。',
    ],
    tests: 'T-EVENT-0001 将 Event callback 作为 runtime User-to-Component 响应入口进行覆盖。',
    source: 'spec/contracts/C-EVENT-0001.yaml',
  },
  {
    id: 'C-EVENT-0002',
    title: 'Event 区分 setup listener registration 与 runtime callback dispatch',
    summary: '`def.event` 在 setup 期间注册 listener；Event 当前不提供原型作者侧 runtime API。',
    quote:
      'The Event API must separate setup-time listener registration from runtime callback dispatch.',
    criteria: [
      '`def.event.on` 与 `def.event.onGlobal` 是 setup-only listener registration API。',
      '`def.event.off(token)` 是 setup-only，不是 runtime unsubscribe API。',
      'callback 只在 runtime dispatch，并接收 `cb(run, ev)`。',
      'Event 通路当前不固化 prototype-author runtime API。',
    ],
    tests:
      'T-EVENT-0001 覆盖 registration 与 dispatch；T-EVENT-0002 覆盖 payload 与 runtime API 边界。',
    source: 'spec/contracts/C-EVENT-0002.yaml',
  },
  {
    id: 'C-EVENT-0003',
    title: 'Event registration 绑定到 root 与 global interaction target',
    summary:
      '`def.event.on` 绑定到组件 root interaction target；`onGlobal` 绑定到 adapter-defined global target。',
    quote: 'Event registrations must bind to interaction targets supplied by the adapter.',
    criteria: [
      '`def.event.on(...)` 默认绑定到当前组件实例 root interaction target。',
      '`def.event.onGlobal(...)` 绑定到 adapter-defined global interaction target。',
      'Event facade 不得向原型作者暴露具体 root/global target 对象。',
      '宿主映射与 target 替换属于 runtime/adapter 职责。',
    ],
    tests: 'T-EVENT-0001 的 target binding case 覆盖 root/global target 行为。',
    source: 'spec/contracts/C-EVENT-0003.yaml',
  },
  {
    id: 'C-EVENT-0004',
    title: '空 event registration 是 binding no-op',
    summary: '没有 registration 时，event binding 不得读取 target，也不得因为 target 缺失而失败。',
    quote: 'Event binding must read targets according to actual registration needs.',
    criteria: [
      '没有 event registration 时，event binding 是 no-op。',
      '没有 event registration 时，binding 不得读取 root 或 global target。',
      '只有存在 root-scoped registration 时才要求 root target。',
      '只有存在 global-scoped registration 时才要求 global target。',
    ],
    tests: 'T-EVENT-0001 的 empty-registration case 覆盖 no-op binding 行为。',
    source: 'spec/contracts/C-EVENT-0004.yaml',
  },
  {
    id: 'C-EVENT-0005',
    title: 'Event registration 独立且不去重',
    summary:
      '每次 `def.event.on` 或 `onGlobal` 调用都创建独立 registration，即使 type、callback 和 options 完全相同。',
    quote: 'Event registration must be modeled by call count.',
    criteria: [
      '每次 root registration 调用创建新的 root-scoped registration。',
      '每次 global registration 调用创建新的 global-scoped registration。',
      '相同 registration 不得被去重、合并或覆盖。',
      '多个相同 registration 在匹配事件发生时产生多个独立 callback delivery。',
    ],
    tests: 'T-EVENT-0001 no-dedup case 与 event router no-dedupe 测试覆盖该行为。',
    source: 'spec/contracts/C-EVENT-0005.yaml',
  },
  {
    id: 'C-EVENT-0006',
    title: 'EventListenerToken 是精确移除句柄',
    summary:
      '每次 event registration 返回一个 token，`def.event.off(token)` 只移除该 token 对应的 registration。',
    quote: 'The portable removal mechanism for event registrations must be token-based.',
    criteria: [
      '每次 registration 调用都返回 `EventListenerToken`。',
      '`EventListenerToken.id` 在当前组件实例内稳定且唯一。',
      '`def.event.off(token)` 只移除该 token 标识的 registration。',
      '若该 registration 已经绑定，`off(token)` 必须立即 detach listener。',
    ],
    tests: 'T-EVENT-0001 token-removal case 覆盖精确移除与 detach 行为。',
    source: 'spec/contracts/C-EVENT-0006.yaml',
  },
  {
    id: 'C-EVENT-0007',
    title: 'Event binding 随 lifecycle cleanup 与 target migration',
    summary:
      'Event binding 必须在 unmount/dispose 时清理，并透明跟随 runtime/adapter target 替换。',
    quote:
      'Event bindings must be automatically cleaned up when the component instance leaves runtime responsibility.',
    criteria: [
      'unmount 或 dispose 必须 detach event binding。',
      'unmount/dispose 后，残留 host event 不得再调用旧 callback。',
      'root/global target 替换后，后续 event binding 必须使用新 target。',
      'target replacement 对原型作者透明。',
    ],
    tests: 'T-EVENT-0001 cleanup/rebind case 覆盖 lifecycle cleanup 与 target migration。',
    source: 'spec/contracts/C-EVENT-0007.yaml',
  },
  {
    id: 'C-EVENT-TYPE-0001',
    title: 'EventTypeV0 是分层语义事件模型',
    summary:
      'event type 分为 portable core event、optional medium event 与 host-bound escape hatch。',
    quote:
      '`EventTypeV0` must be modeled as a layered semantic event model, not as an event-name mapping table for a particular host platform.',
    criteria: [
      '`EventTypeV0` 不得是 DOM、Web、设备或任一具体 host event name 的别名。',
      '`EventTypeV0` 必须区分 core、optional 与 host-bound 三层。',
      'core event 承载最强可移植语义；host-bound event 放弃跨 host 语义。',
      'adapter 负责把 host input system 解释到其支持的 event layer。',
    ],
    tests: 'T-EVENT-0002 type validation case 覆盖 event-type layer 边界。',
    source: 'spec/contracts/C-EVENT-TYPE-0001.yaml',
  },
  {
    id: 'C-EVENT-TYPE-0002',
    title: 'Protocol core event 表达 portable interaction intent',
    summary: '`press.*` 与 `key.*` 描述 portable interaction intent，而不是具体设备或宿主事件。',
    quote:
      'Protocol core events must describe cross-host user interaction intent rather than concrete devices, frameworks, or host events.',
    criteria: [
      'v0 core set 包含 `press.start`、`press.end`、`press.cancel`、`press.commit`、`key.down` 与 `key.up`。',
      '`press.*` 表达 activation intent 生命周期。',
      '`key.down` 与 `key.up` 表达 input channel 的按下与释放。',
      'core event 不得被定义为 DOM `click`、`keydown` 或 `keyup` 的别名。',
    ],
    tests: 'T-EVENT-0002 覆盖 core type validation 与 key payload case。',
    source: 'spec/contracts/C-EVENT-TYPE-0002.yaml',
  },
  {
    id: 'C-EVENT-TYPE-0003',
    title: 'Optional medium event 是条件支持的语义事件',
    summary:
      'optional event 描述 pointer、focus、text、input/change 与 context-menu 等 adapter 可选支持的语义。',
    quote: 'Optional medium events sit between protocol core events and host-bound escape hatches.',
    criteria: [
      'optional medium event 不要求所有 adapter 都支持。',
      '若 adapter 支持某个 optional event，必须保持该 event family 的语义。',
      'v0 pointer set 包含 `pointer.down/move/up/cancel/enter/leave`。',
      'v0 focus/input set 包含 `nav.*`、`text.*`、`input`、`change` 与 `context.menu`。',
    ],
    tests: 'T-EVENT-0002 optional payload 与 type validation case 覆盖部分 optional events。',
    source: 'spec/contracts/C-EVENT-TYPE-0003.yaml',
  },
  {
    id: 'C-EVENT-TYPE-0004',
    title: 'host event type 是 host-bound escape hatch',
    summary: '`host:*` 保留 Event lifecycle 保证，但放弃 portable event semantics。',
    quote: '`host:*` event types are host-bound escape hatches in the Event channel.',
    criteria: [
      'host-bound event escape hatch 的规范前缀是 `host:`。',
      '`host:*` event type 不承诺跨 host 语义一致性。',
      '`host:*` 仍遵守 setup registration、callback dispatch、cleanup 与 target binding 规则。',
      'payload、命名、触发条件与具体 host mapping 均由 adapter 定义。',
    ],
    tests: 'T-EVENT-0002 host-bound case 覆盖 `host:*` 前缀与 lifecycle-only 保证。',
    source: 'spec/contracts/C-EVENT-TYPE-0004.yaml',
  },
  {
    id: 'C-EVENT-TOKEN-0001',
    title: 'Event token 暴露稳定诊断 metadata',
    summary: 'token metadata 只用于诊断，不得影响 matching、dispatch 或 removal。',
    quote: '`EventListenerToken` must carry minimal readable metadata.',
    criteria: [
      '`meta.kind` 是 `root` 或 `global`，并与 registration scope 一致。',
      '`meta.type` 记录原始 event type string。',
      '`options` 与 `label` 不得影响 matching、dispatch、deduplication 或 removal。',
      '`token.desc(text)` 是 setup-only，并返回同一个 token instance。',
    ],
    tests: 'T-EVENT-0001 token-removal case 覆盖部分 token metadata anchor。',
    source: 'spec/contracts/C-EVENT-TOKEN-0001.yaml',
  },
];

## Event 是什么？

`Event` 是 Proto UI 的 User → Component 信息通路。它把用户操作、输入与交互意图带入组件，让组件能在 runtime callback 中响应用户行为。

没有 Event 通路，组件无法得知何时发生了交互，也无法对 User 的交互产生反应。

## Setup Registration 与 Runtime Dispatch

Event 遵守 Proto UI 通用的 `setup` / `runtime` 分离，但 Event 本身没有原型作者侧 runtime API。你只能在 setup 期计划如何订阅交互事件，而这些订阅回调只会在 runtime 期间执行。

<SpecPhaseBadge label="setup-only" />

原型作者在 setup 期间声明自己关心哪些交互：

```ts
def.event.on('press.commit', (run, ev) => {
  run.update();
});

def.event.onGlobal('key.down', (run, ev) => {
  if (ev.key === 'Escape') run.update();
});
```

`def.event.on`、`def.event.onGlobal`、`def.event.off` 与 `token.desc` 都是 setup-only。registration API 不会在注册时立即执行 callback。`off(token)` 只移除该 token 代表的 registration；它不是 runtime unsubscribe API。

当 interaction event 在 runtime 期间被 dispatch 时，已注册 callback 接收：

```ts
(run, ev) => void
```

`run` 绑定到当前 callback invocation。Event module 自身不构造也不校验这个 handle；绑定正确 handle 是 runtime 的职责。payload `ev` 则在 event type 承诺的范围内携带 portable event 信息。

主动触发 event、runtime 动态订阅或 runtime 取消订阅如果未来提供，也会是独立 escape hatch，不属于 v0 Event channel。

## Binding Targets

Event registration 绑定到 adapter 提供的 interaction target：

```ts
def.event.on('press.commit', onCommit); // root-scoped
def.event.onGlobal('key.down', onKeyDown); // global-scoped
```

`def.event.on(...)` 默认绑定到当前组件实例的 root interaction target。`def.event.onGlobal(...)` 绑定到 adapter-defined global interaction target。原型作者不会拿到这些具体 target，也不决定 host 使用 DOM node、`window`、framework root、native view 还是其他机制。

binding 是按需的。若组件没有任何 event registration，event binding step 就是 no-op，不得要求 root 或 global target 可用。只有存在 root-scoped registration 时才要求 root target；只有存在 global-scoped registration 时才要求 global target。

如果 setup 期间先注册某个事件、随后又通过 token 将其关闭，最终进入 runtime 时仍然等价于没有任何 event registration，也不应产生运行时 event binding 开销。这让 setup logic 可以临时建立并撤销 event plan，而不会留下 runtime listener。

root-scoped target 默认指向组件 Root Node，且进入 runtime 后不可变更。setup 期间，特权基础设施可以把它转移到另一个 interaction target。当前公开使用场景是 `asTrigger`：它允许类似 trigger 的子结构把 event ownership 转移给父级 trigger target；这个转移能力并不作为通用原型作者 API 暴露。

## Event Type 分层

`EventTypeV0` 是语义模型，不是 DOM event name 列表。

```ts
type EventTypeV0 = CoreEventType | OptionalEventType | `host:${string}`;
```

分层如下：

| Layer | Examples | Portability |
| --- | --- | --- |
| protocol core | `press.commit`, `key.down`, `key.up` | 最强 portable semantics |
| optional medium | `pointer.down`, `context.menu`, `input` | adapter 支持该 medium 时具备语义 |
| host-bound escape hatch | `host:click`, `host:pointerdown` | 只保留 lifecycle 保证；语义由 host 定义 |

Core event 表达 portable interaction intent。`press.commit` 表示一次有效 activation 已确认完成；它不是 DOM `click` 的同义词。`key.down` 与 `key.up` 描述 input channel 的按下与释放，也不被限定为物理键盘事件。

Optional medium event 更接近 pointer、focus、text input 或 context menu 这样的交互媒介。adapter 可以不支持所有 optional event；但一旦宣称支持，就必须保持对应 event family 的语义。

`host:*` 是明确的 escape hatch。它保留 setup registration、runtime callback dispatch、lifecycle cleanup 与 target binding 等 Event 保证，但放弃跨 host event semantics。payload shape、命名、触发条件与具体 host mapping 都由 adapter 定义。

## Payload Shape

portable Event payload 至少必须暴露产生该 dispatch 的 portable event type：

```ts
def.event.on('press.commit', (run, ev) => {
  ev.type; // 'press.commit'
});
```

对 `key.*` 而言，payload 还必须暴露 portable `key` string：

```ts
def.event.onGlobal('key.down', (run, ev) => {
  if (ev.key === 'Enter') {
    run.update();
  }
});
```

adapter 可以暴露 native event object、具体 target、`preventDefault` 或 `stopPropagation` 这类 host-local escape hatch，但这些字段不是 portable Event guarantee。依赖它们的代码是在依赖某个 host profile，而不是跨 host Event 契约。

## Lifecycle 与 Cleanup

Event binding 由 lifecycle 管理。当组件实例 unmount 或 dispose 时，binding 必须 detach，确保残留 host event 不能继续调用旧 callback。

如果 adapter 重新挂载或替换了某个已计划 target 背后的具体 host object，binding 必须透明跟随。原型作者不应该需要持有 target 引用，也不需要手动更新 subscription。

重复 registration 不会被去重。如果原型对同一个 event type 注册同一个 callback 两次，就存在两条独立 registration，也会有两次 callback delivery。移除是 token-based，因此每条 registration 都能被精确移除。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Event 覆盖映射到以下测试实体：

| 测试实体 | 主要覆盖 |
| --- | --- |
| `T-EVENT-0001` | setup registration、runtime dispatch、target binding、no-op binding、no deduplication、token removal、cleanup/rebind |
| `T-EVENT-0002` | EventTypeV0 validation、portable payload shape、`host:*`、runtime API boundary |

这些测试把 Event 从“host 发了什么就是什么”收敛为一个具备明确 portability layer 与 lifecycle 行为的可验证输入通路。

## 与其他规范的关系

- `Core` 定义信息通路、setup/runtime 与 run handle 基础；Event 是其中 User → Component 的具体通路。
- `Lifecycle` 定义 runtime callback 可用性、cleanup 与 no-implicit-render 边界；Event callback 必须遵守它。
- `Feedback` 承载 event 之后组件向 User 产生的输出；Event 本身不表达用户感知到什么。
- `State`、`Props`、`Context` 可以在 event callback 中通过各自 runtime API 被读取或修改，但它们仍是独立信息通路。
