---
title: 'Feedback'
desp: 'Feedback 规范'
description: 'Feedback 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-FEEDBACK-0001',
    title: 'Feedback 是 Component 到 User 的信息通路',
    summary:
      'Feedback 承载组件向 User 传递的可感知信息，让 User 感知组件的存在、状态、变化与交互结果。',
    quote:
      'Feedback is the Component → User information channel in Proto UI, and belongs only to that direction.',
    criteria: [
      'Feedback 必须被识别为 Component → User 通路。',
      'Feedback 不得被解释为 App Maker 配置、组件间协作或 host/environment 通路。',
      '没有 feedback 时，User 无法通过 Proto UI portable semantics 感知组件。',
    ],
    tests: '作为通路身份契约，被 feedback.style authoring 与 runtime patch 测试间接承接。',
    source: 'spec/contracts/C-FEEDBACK-0001.yaml',
  },
  {
    id: 'C-FEEDBACK-0002',
    title: 'Feedback 区分 setup planning 与 runtime effects',
    summary:
      'setup API 计划组件如何产生反馈；runtime API 表达运行期反馈副作用，不重新打开 setup plan。',
    quote:
      'Feedback APIs must distinguish setup-time feedback planning from runtime feedback effects.',
    criteria: [
      'setup-time API 用于声明或计划反馈能力。',
      '进入 runtime 后，setup-time feedback planning API 不得继续可用。',
      'runtime feedback effect 不得改变 template 逻辑结构或内容。',
      'runtime feedback effect 应能不触发 render 地更新反馈。',
    ],
    tests:
      'T-FEEDBACK-STYLE-0001 覆盖 setup-only style planning；T-FEEDBACK-STYLE-0002 覆盖 runtime style patch effect。',
    source: 'spec/contracts/C-FEEDBACK-0002.yaml',
  },
  {
    id: 'C-FEEDBACK-STYLE-0001',
    title: 'feedback.style 是视觉反馈 surface',
    summary:
      '`feedback.style` 描述组件外观变化，让 User 感知组件存在、状态或变化，但不改变 template 结构。',
    quote:
      '`feedback.style` describes visual feedback that can change component appearance without changing template structure.',
    criteria: [
      '`feedback.style` 只覆盖视觉反馈。',
      '它可以改变组件视觉样貌。',
      '它不得表达 template 逻辑树、结构或内容变化。',
      '宿主实现方式由 adapter 或翻译层决定。',
    ],
    tests: 'T-FEEDBACK-STYLE-0001 的 setup authoring cases 覆盖视觉反馈 surface 与结构边界。',
    source: 'spec/contracts/C-FEEDBACK-STYLE-0001.yaml',
  },
  {
    id: 'C-FEEDBACK-STYLE-0002',
    title: 'Setup style plan 是 setup-only 且仅 setup 期可撤销',
    summary:
      '`def.feedback.style.use(...)` 在 setup 阶段登记 style plan；返回的 `unUse` 只用于 setup 期撤销该贡献。',
    quote:
      'Setup-time `feedback.style` records style plans, and each setup plan contribution can be removed only during setup.',
    criteria: [
      'setup style plan 通过 `def.feedback.style.use(...handles)` 登记。',
      '`def.feedback.style.use` 是 setup-only。',
      '`unUse` 只移除对应 use 调用的贡献。',
      '`unUse` 不是 runtime update 机制，也不是 lifecycle disposer。',
    ],
    tests:
      'T-FEEDBACK-STYLE-0001 与 runtime feedback.style setup-only 测试覆盖 use/unUse 阶段边界。',
    source: 'spec/contracts/C-FEEDBACK-STYLE-0002.yaml',
  },
  {
    id: 'C-FEEDBACK-STYLE-0003',
    title: 'feedback.style value 是 style token set',
    summary:
      '作者侧 style token 表达原型内部视觉意图，不等同于最终 host class、CSS rule 或 adapter 产物。',
    quote: '`feedback.style` values are sets of prototype-author-side Proto UI style tokens.',
    criteria: [
      '`feedback.style` value 由作者侧 style token 集合表达。',
      'v0 token 语法是 Tailwind atomic class 语法的子集。',
      'Proto UI style token 不要求与最终宿主样式产物一一对应。',
      '翻译层可映射到 CSS、attribute、decorator 或 host-private structure。',
    ],
    tests:
      'T-FEEDBACK-STYLE-0001 覆盖 author token value boundary；T-FEEDBACK-STYLE-0002 覆盖 runtime patch 输入边界。',
    source: 'spec/contracts/C-FEEDBACK-STYLE-0003.yaml',
  },
  {
    id: 'C-FEEDBACK-STYLE-0004',
    title: '作者侧 style token 不承载状态或 selector 逻辑',
    summary: 'style token 必须只表达样式意图，不得混入状态、事件、selector 或宿主实现依赖。',
    quote:
      'Prototype-author-side `feedback.style` tokens must describe style only and must not encode state, event, selector, or host-realization dependencies.',
    criteria: [
      '作者侧 style token 不得包含 `:`。',
      '不得直接表达 hover、active、focus、data selector 或 ARIA selector。',
      '状态化视觉反馈应通过 rule 与 props/state/context 等信息通路组合表达。',
      'adapter 生成的最终样式产物不受作者侧 token 语法约束。',
    ],
    tests:
      'T-FEEDBACK-STYLE-0001 token purity case 与 T-FEEDBACK-STYLE-0002 patch token purity case 覆盖该边界。',
    source: 'spec/contracts/C-FEEDBACK-STYLE-0004.yaml',
  },
  {
    id: 'C-FEEDBACK-STYLE-0005',
    title: 'Runtime style patch 是翻译前的 feedback.style escape hatch',
    summary:
      '`run.feedback.style.patch/suppress/clearPatch` 修改当前实例唯一 runtime style patch layer。',
    quote:
      '`run.feedback.style.patch`, `suppress`, and `clearPatch` modify the current runtime style patch layer before style translation.',
    criteria: [
      'API 固定为 `patch`、`suppress` 与 `clearPatch`，且是 runtime-only。',
      'patch layer 在 setup style plan 与 rule style intent 之后、翻译层之前生效。',
      '同一 semantic group 后写覆盖前写，不同 group 互不影响。',
      'patch 只触发 style flush，不应触发 render，也不得改变 template 结构。',
    ],
    tests:
      'T-FEEDBACK-STYLE-0002 覆盖 patch、suppress、clearPatch、token purity 与 no-render style flush。',
    source: 'spec/contracts/C-FEEDBACK-STYLE-0005.yaml',
  },
];

## Feedback 是什么？

`Feedback` 是 Proto UI 的信息通路之一，方向是 Component → User。它承载组件向用户传递的可感知信息，让用户能够感知组件的存在、状态、变化和交互结果。

没有 feedback 通路，用户无法通过 Proto UI 的 portable semantics 感知组件。组件可以接收 props、处理 event、维护 state、向 App Maker 暴露能力，但如果没有任何反馈，User 看不到、听不到、摸不到，也无法理解组件发生了什么。

Feedback 不等同于“渲染”。render 负责产出 template 结构；feedback 负责组件如何被用户感知。很多 feedback 最终会通过视觉呈现实现，但它在语义上是 Component → User 的信息通路，而不是某个 host 样式机制的别名。

## Setup Planning 与 Runtime Effects

Feedback API 遵守 `setup` / `runtime` 分离。

<SpecPhaseBadge label="setup-only" />

setup 期 API 用来声明或计划组件将如何产生反馈。以 `feedback.style` 为例：

```ts
def.feedback.style.use(tw('inline-flex items-center opacity-100'));
```

这不是立即修改某个真实 host node，而是在 setup 阶段登记 style plan。`use()` 返回的 `unUse` 也只用于 setup 期撤销该次贡献；它不是 runtime update 机制，也不是 lifecycle disposer。

<SpecPhaseBadge label="runtime-only" />

runtime 期 API 表达的是运行期反馈副作用。v0 当前公开的是 `feedback.style` 的 patch escape hatch：

```ts
def.lifecycle.onMounted((run) => {
  run.feedback.style.patch(tw('opacity-50'));
});
```

runtime feedback effect 不得改变 template 逻辑结构或内容。它可以更新反馈，理想情况下只触发 style flush；是否进入 render/update flow 仍由 Lifecycle / runtime update 语义管理。

## feedback.style

`feedback.style` 是当前 v0 编目的视觉反馈 surface。它描述组件应该如何改变自身样貌，让 User 感知组件的状态、变化或交互结果。

```ts
def.feedback.style.use(tw('rounded-md bg-primary text-primary-foreground'));
```

这些 token 是原型作者侧的 Proto UI style token，表达的是原型内部视觉意图。它们不是 DOM class、不是 CSS rule、不是 `data-pui-style` token，也不要求和最终宿主产物一一对应。

翻译层可以把 style token 映射为 CSS、attribute、样式装饰器、host-private structure，甚至完全不同的宿主机制。只要保持 `feedback.style` 的语义意图，最终产物的结构由 adapter 或翻译层负责。

## Style Token 纯度

作者侧 style token 只能表达样式意图，不能把状态、事件、selector 或 host realization 混进去。

```ts
tw('opacity-50'); // valid
tw('hover:opacity-50'); // invalid
tw('data-[open]:opacity-50'); // invalid
```

禁止 `:` 不是 parser 能力不足，而是 feedback 通路纯度要求。`hover:bg-white` 把状态逻辑塞进了 style token；Proto UI 希望这种关系通过更明确的信息通路和 API 表达。

如果要表达“当 hover 时变透明”，推荐路径是用 rule 组合 state、props、context 等信息源与 feedback intent，而不是让 style token 自带状态。adapter 或专门的 rule extension 可以在最终宿主产物中生成 selector 或 data attribute，但那已经是翻译层结果，不再是原型作者侧 style token。

## Runtime Style Patch

`run.feedback.style.patch/suppress/clearPatch` 是 runtime-only escape hatch。它不是推荐的动态样式主路径；推荐路径仍然是 rule + feedback + props/state/context。patch 存在是为了处理少数无法自然声明成 rule 的运行期反馈修补。

```ts
run.feedback.style.patch(tw('opacity-100'));
run.feedback.style.suppress(tw('bg-primary'));
run.feedback.style.clearPatch();
```

每个实例只有一份 runtime style patch layer。`patch()` 写入正向补丁；`suppress()` 写入负向补丁；同一 semantic group 后写覆盖前写，不同 semantic group 互不影响。`clearPatch()` 清空 patch layer，使最终结果回到 setup 与 rule 产生的基础 style 语义结果。

patch layer 的位置很关键：

```txt
setup style plan
  -> rule style intent
  -> runtime style patch
  -> translation layer
  -> host artifact
```

也就是说，patch 对 setup/rule 之后的最终可翻译 style 语义结果负责，但不直接覆盖翻译层之后的 host 产物。adapter 必须消费应用 patch 后的 semantic result，而不是绕过 patch 直接消费 setup 或 rule 的中间结果。

## Feedback-only Template 结构

Template 允许局部节点携带 `style`，这是 feedback-only 子结构可以不拆独立原型的一种体现。style 只能激活视觉反馈能力，不能顺便激活 props、event、expose、context 或 state。

如果一个子结构需要独立接收配置、处理事件、暴露能力、共享上下文或维护自己的状态，它就不再只是 feedback-only，通常应该被拆成独立原型。Feedback 的弹性不应被解释为 template 可以嵌套任意原型。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Feedback 的测试映射当前集中在 `feedback.style`：

| 测试实体 | 主要覆盖 |
| --- | --- |
| `T-FEEDBACK-STYLE-0001` | setup style authoring、use/unUse、author token、token purity |
| `T-FEEDBACK-STYLE-0002` | runtime patch、suppress、clearPatch、patch token purity、no-render style flush |

这些测试把 feedback.style 从“样式最终长什么样”收敛为“原型作者侧语义 token 如何进入 setup/rule/patch/translation 这条链路”。

## 与其他规范的关系

- `Core` 定义信息通路、setup/runtime 与 run handle 基础；Feedback 是其中 Component → User 的具体通路。
- `Template` 允许节点携带静态 `style`，但 style 语义由 `feedback.style` 定义。
- `Lifecycle` 规定 feedback runtime effect 不应隐式触发 render；style patch 只请求 style flush。
- `Rule` 不是 feedback 的一部分，但可以表达“当某条件成立时执行 feedback intent”的声明式副作用。
- `Props`、`State`、`Context` 可以作为动态反馈的条件来源；Feedback 负责最终传达给 User 的可感知结果。
