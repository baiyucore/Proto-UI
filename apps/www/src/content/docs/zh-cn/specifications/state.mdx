---
title: 'State'
desp: 'State 规范'
description: 'State 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-STATE-0001',
    title: 'State 是 host-neutral 内部状态表达',
    summary:
      'State 用于定义、存储并记录 Component 内部状态随时间发生的变化；它不是信息通路，也不是副作用机制。',
    quote:
      'State defines, stores, and records changes to a Component internal state over time; it is not an information channel.',
    criteria: [
      'State 是 Component 的内部维度，不是 User、App Maker、Other Component 或 host/environment 之间的信息通路。',
      'State API 自身不得隐式触发 render、commit、feedback flush 或其他宿主副作用。',
    ],
    tests: 'T-STATE-0001 通过 no-implicit-render 行为覆盖 State 的无副作用边界。',
    source: 'spec/contracts/C-STATE-0001.yaml',
  },
  {
    id: 'C-STATE-0002',
    title: 'State definition 是 setup-only',
    summary: '`def.state.*` 在 setup 期间定义状态，并返回 state slot 的受控 view。',
    quote:
      'The prototype-author-facing State definition API is def.state.*, whose only core form is defining a state during setup.',
    criteria: [
      '`def.state.*` 只能在 setup 期间调用。',
      '定义 API 的产物必须是 view，而不是裸 state 实例或数据源。',
      '当前原型直接创建并完全控制的 state 必须返回 owned view。',
    ],
    tests: 'T-STATE-0001 覆盖 setup-only definition、owned view 与 phase guard。',
    source: 'spec/contracts/C-STATE-0002.yaml',
  },
  {
    id: 'C-STATE-0003',
    title: 'State slot 通过 capability view 访问',
    summary:
      '原型作者不会拿到裸 state slot；他们拿到 owned、borrowed 或 observed view。App Maker 通过 expose 拿到的是翻译层定义的外部 view。',
    quote:
      'Prototype authors receive capability views over the slot: owned, borrowed, or observed.',
    criteria: [
      '原型作者只能获得受控 state view。',
      'view 能力集合由 view 类型决定，runtime 不得动态升级权限。',
      'App Maker exposed state 不等同于原型作者侧 view。',
    ],
    tests: 'T-STATE-0002 覆盖 owned、borrowed、observed 与 exposed state view 边界。',
    source: 'spec/contracts/C-STATE-0003.yaml',
  },
  {
    id: 'C-STATE-0004',
    title: 'State view API 遵守 setup/runtime phase boundary',
    summary:
      '`get` 在 setup/runtime 都可用；`setDefault` 与 `watch` 是 setup-only；`set` 是 runtime-callback-only。',
    quote:
      'get is available during both setup and runtime, setDefault and watch are setup-only, and set is runtime-callback-only.',
    criteria: [
      '`get()` setup/runtime 都可读取当前值，setup 期间总是读取当前默认值。',
      '`setDefault(...)` 与 `watch(...)` 是 setup-only。',
      '`set(...)` 是 runtime-callback-only。',
    ],
    tests: 'T-STATE-0001 覆盖 owned view phase guard；T-STATE-0004 覆盖 watch setup-only。',
    source: 'spec/contracts/C-STATE-0004.yaml',
  },
  {
    id: 'C-STATE-0005',
    title: 'State mutation 不隐式 render',
    summary: '`set(...)` 只改变并记录 state value，不自动进入 render、commit 或 update flow。',
    quote: 'Calling set(...) must not implicitly enter render, commit, or update flow.',
    criteria: [
      '`set(...)` 不得自动调用 `run.update()`。',
      'created callback 中的 state mutation 必须对首次 render 可见。',
      'mounted 后的 mutation 必须保持已提交输出不变，直到显式 update 或其他声明机制生效。',
    ],
    tests: 'T-STATE-0001 覆盖 created 可见性、mounted no-auto-render 与 explicit update。',
    source: 'spec/contracts/C-STATE-0005.yaml',
  },
  {
    id: 'C-STATE-0006',
    title: 'State definition 使用有限 host-neutral value spec',
    summary:
      'v0 只承认 `bool`、`enum`、`string`、`number.range` 与 `number.discrete`，且值域保持 JSON-compatible。',
    quote:
      'State definition admits only a finite set of host-neutral state kinds, and values remain within JSON-compatible boundaries.',
    criteria: [
      'v0 state kind 限定为 bool、enum、string、number.range 与 number.discrete。',
      'enum 必须声明 options。',
      'numberRange 必须声明 min 与 max。',
      'metadata 仍必须保持 host-neutral。',
    ],
    tests: 'T-STATE-0003 覆盖有限 kind 与 spec metadata；value-domain validation 仍是记录的断口。',
    source: 'spec/contracts/C-STATE-0006.yaml',
  },
  {
    id: 'C-STATE-0007',
    title: 'Owned state view 表示直接原型所有权',
    summary:
      'Owned view 用于当前原型直接创建并完全控制的 state；它能 `set`，但故意不提供 `watch`。',
    quote:
      'The owned view may read, set defaults, and set the current value during runtime, but must not expose watch.',
    criteria: [
      'Owned view 必须提供 `get()`、setup-only `setDefault(...)` 与 runtime-only `set(...)`。',
      'Owned view 不得提供 prototype-author-facing `watch(...)`。',
    ],
    tests: 'T-STATE-0001 与 T-STATE-0002 覆盖 owned view 能力集合。',
    source: 'spec/contracts/C-STATE-0007.yaml',
  },
  {
    id: 'C-STATE-0008',
    title: 'Borrowed state view 表示共享控制',
    summary: 'Borrowed view 用于当前原型可控制但不完全拥有的 state；它既能 `set`，也能 `watch`。',
    quote:
      'Borrowed views fit states that are controllable but not fully owned, such as asHook state and official interaction semantic state.',
    criteria: [
      'Borrowed view 必须提供 `get()`、setup-only `setDefault(...)`、runtime-only `set(...)` 与 setup-only `watch(...)`。',
      '适用于 asHook state 与官方 interaction state 等“可控制但不完全拥有”的状态。',
    ],
    tests:
      'T-STATE-0002 覆盖 borrowed view；T-STATE-INTERACTION-0001 覆盖 interaction state borrowed identity。',
    source: 'spec/contracts/C-STATE-0008.yaml',
  },
  {
    id: 'C-STATE-0009',
    title: 'Observed state view 表示只读观察',
    summary: 'Observed view 用于当前原型完全不能控制的 state；它只能读取和观察，不能设置。',
    quote: 'Observed views must not provide setDefault or set, preserving a read-only boundary.',
    criteria: [
      'Observed view 必须提供 `get()` 与 setup-only `watch(...)`。',
      'Observed view 不得提供 `setDefault(...)` 或 `set(...)`。',
    ],
    tests: 'T-STATE-0002 覆盖 observed view 能力集合。',
    source: 'spec/contracts/C-STATE-0009.yaml',
  },
  {
    id: 'C-STATE-0010',
    title: 'State watch 是 setup-only auxiliary observation',
    summary: '`watch(...)` 用于观察 borrowed/observed state 的变化，但它不是响应式渲染系统。',
    quote:
      'State watch is not part of the owned view, does not establish render dependencies, and does not implicitly trigger update.',
    criteria: [
      '`watch(...)` 只存在于 borrowed 与 observed view。',
      'watch registration 必须是 setup-only。',
      'watch callback 不得隐式触发 render、commit 或 update。',
    ],
    tests: 'T-STATE-0004 覆盖 watch setup-only、callback delivery 与 lifecycle cleanup。',
    source: 'spec/contracts/C-STATE-0010.yaml',
  },
  {
    id: 'C-STATE-0011',
    title: 'StateEvent 记录 value change 与 disconnect',
    summary:
      'watcher 在 value 实际变化时收到 `next` event；生命周期断开时可以收到 `disconnect` event。',
    quote: 'StateEvent is the portable event shape for state-change records.',
    criteria: [
      'value transition 必须派发 `{ type: "next", prev, next, reason? }`。',
      '`Object.is(prev, next)` 为 true 时不得视为实际 state change。',
      '`setDefault(...)` 不得派发 `next` event。',
      're-entrant `set(...)` 触发的 StateEvent 必须以确定性顺序派发。',
    ],
    tests:
      'T-STATE-0004 覆盖 StateEvent shape、same-value suppression、setDefault no-event 与 re-entrant order。',
    source: 'spec/contracts/C-STATE-0011.yaml',
  },
  {
    id: 'C-STATE-0012',
    title: 'State view 绑定到 instance lifecycle',
    summary:
      '实例存活期间 state view 按 phase rule 可用；dispose 后所有 view API 与 watcher 都必须失效。',
    quote:
      'State views remain usable while the instance is alive and become invalid after dispose.',
    criteria: [
      'unmounted callback 执行期间实例仍被视为存活。',
      'dispose 后调用 `get()`、`setDefault(...)`、`set(...)` 或 `watch(...)` 必须失败。',
      'dispose 必须清理 watch registration 与内部 subscription。',
    ],
    tests: 'T-STATE-0004 与 runtime lifecycle contract 覆盖 alive/dispose 边界。',
    source: 'spec/contracts/C-STATE-0012.yaml',
  },
  {
    id: 'C-STATE-INTERACTION-0001',
    title: 'Interaction state 是 state + event 的复合 state 子领域',
    summary: 'Interaction state 暴露官方交互语义状态，并通过 event/module/runtime 自动维护。',
    quote:
      'Interaction state is a State subdomain produced by combining State with Event-driven interaction semantics.',
    criteria: [
      'Interaction state 必须归入 State 子领域，而不是普通 Event callback。',
      '同一个交互主体上的同类 interaction state 必须共享同一份 state reference。',
    ],
    tests: 'T-STATE-INTERACTION-0001 覆盖 shared subject identity。',
    source: 'spec/contracts/C-STATE-INTERACTION-0001.yaml',
  },
  {
    id: 'C-STATE-INTERACTION-0002',
    title: 'Interaction state 由 runtime-managed event wiring 维护',
    summary:
      '声明 interaction state 会隐式引入 runtime-managed event subscriptions，由 module/runtime/adapter 协同维护。',
    quote:
      'Interaction state declaration introduces runtime-managed event wiring rather than prototype-authored event subscriptions.',
    criteria: [
      'interaction state 更新必须发生在原型作者 event callback 观察之前。',
      'disabled state 必须 gate transient interaction state。',
      'focus-visible 必须遵守 keyboard/pointer modality。',
    ],
    tests:
      'T-STATE-INTERACTION-0001 覆盖 event wiring、callback order、disabled gate 与 focus-visible modality。',
    source: 'spec/contracts/C-STATE-INTERACTION-0002.yaml',
  },
  {
    id: 'C-STATE-INTERACTION-0003',
    title: 'Interaction state identity 按交互主体共享',
    summary:
      '同一交互主体上重复获取同名 interaction state 必须得到同一份状态引用，asHook 不需要额外导出这些 state。',
    quote:
      'Repeated access to the same interaction state on the same interaction subject must return the same state reference.',
    criteria: [
      '同一 subject + 同一 interaction name 返回同一引用。',
      '不同 subject 的 interaction state 不得共享内存引用。',
      'asHook 内部创建的 interaction state 可由调用方直接重新获取。',
    ],
    tests: 'T-STATE-INTERACTION-0001 shared subject case 覆盖 identity 规则。',
    source: 'spec/contracts/C-STATE-INTERACTION-0003.yaml',
  },
];

## State 是什么？

`State` 是 Proto UI 提供的 host-neutral 状态表达方式。它用于定义状态、存储状态，并记录状态随时间发生的变化。

State 不是信息通路。它不负责把 App Maker 的配置送入组件，不负责把组件结果反馈给用户，也不负责组件之间共享环境。它表达的是 Component 自己在时间中的内部连续性。

State 也不是副作用机制。修改 state 不会隐式触发 render、commit、feedback flush 或任何宿主副作用。状态变化会被记录下来；要让 UI 反映变化，需要显式 update、rule、feedback 或其他契约定义的路径。

## Definition 与 State Machine

State 的原型作者侧入口是 `def.state.*`。从 API 形状上看，它唯一的核心动作是“定义一个状态”，而这个动作是 setup-only。

<SpecPhaseBadge label="setup-only" />

```ts
const open = def.state.bool('open', false);
const placement = def.state.enum('placement', 'bottom', {
  options: ['top', 'right', 'bottom', 'left'] as const,
});
```

`def.state.*` 不会把一次运行时数据写进组件。它定义一个 state slot，并返回这个 slot 的受控 view。这个 view 背后是一个状态机：它有默认值、当前值、变更记录、watcher 与生命周期边界。

State definition 完成之后，state view 会跨 setup 与 runtime 存在。view 上的不同 API 有不同 phase 规则：

| API                   | Phase                 | 语义                                  |
| --------------------- | --------------------- | ------------------------------------- |
| `get()`               | setup + runtime       | 读取当前值；setup 期间读取当前默认值  |
| `setDefault(value)`   | setup-only            | 设置默认值                            |
| `set(value, reason?)` | runtime callback-only | 在运行期改变当前值                    |
| `watch(callback)`     | setup-only            | 注册对 borrowed/observed state 的观察 |

`get()` 是特殊的：它在 setup 与 runtime 都可用。setup 期间还没有真实 runtime mutation，因此读到的是当前默认值；进入 runtime 后，它读到状态机的当前值。

## State View

原型作者不会拿到裸 state slot 或裸数据源。App Maker 即使通过 expose 得到 state，也不会得到这个内部数据源。所有访问都通过 view 完成。

原型作者侧有三种 view：

| View       | 适用场景                           | 能力                                |
| ---------- | ---------------------------------- | ----------------------------------- |
| `owned`    | 当前原型创建并完全控制该 state     | `get`、`setDefault`、`set`          |
| `borrowed` | 当前原型可控制但不完全拥有该 state | `get`、`setDefault`、`set`、`watch` |
| `observed` | 当前原型完全不能控制该 state       | `get`、`watch`                      |

### Owned View

Owned view 表示这个 state 由当前原型直接创建，并且完全受当前原型控制。

```ts
const open = def.state.bool('open', false);

def.event.on('press.commit', () => {
  open.set(!open.get());
});
```

Owned view 故意不提供 `watch()`。这是一个主动设限：原型作者不应该把自己完全控制的状态变化写成监听副作用。若需要让 state 变化影响输出，应显式调用 update、使用 rule，或通过更明确的协作机制表达。

### Borrowed View

Borrowed view 表示当前原型可以控制这个 state，但它不是完全且直接的所有者。常见例子是 asHook state 与官方 interaction state。

```ts
const pressed = def.state.fromInteraction('pressed');

pressed.watch((run, event) => {
  if (event.type === 'next') {
    run.update();
  }
});
```

Borrowed view 既能 `set()`，也能 `watch()`。它适合“可控制但不完全拥有”的状态：当前原型可以影响它，但不应该把它解释成完全私有状态。

### Observed View

Observed view 表示当前原型完全不能控制这个 state，只能读取和观察它。它不提供 `setDefault()` 或 `set()`。

Observed view 适合事实来源在别处的状态。当前原型可以响应它，但不能改写它。

## Value Spec

State definition 只承认有限的 host-neutral state kind：

| API                        | Kind              | 要求                                           |
| -------------------------- | ----------------- | ---------------------------------------------- |
| `def.state.bool`           | `bool`            | boolean value                                  |
| `def.state.enum`           | `enum`            | 必须声明 `options`                             |
| `def.state.string`         | `string`          | 可用 `options` 等 metadata 描述值域            |
| `def.state.numberRange`    | `number.range`    | 必须声明 `min` 与 `max`                        |
| `def.state.numberDiscrete` | `number.discrete` | 可用 `min`、`max`、`step` 等 metadata 描述值域 |

这些 state value 仍然需要满足 JSON-compatible value 边界。State 不要求 function、symbol、class instance、DOM object 或其他 host object 才能表达语义。

metadata 也必须保持 host-neutral。比如 enum options、number range、step 都是 portable value-domain 描述；它们不应该绑定到某个宿主控件或宿主对象。

当前 value-domain validation 仍是治理断口：契约已经要求定义值域，但运行期是否对所有 definition、`setDefault` 与 `set` 执行完整校验，还没有被当作已完成保证。

## Watch 与 StateEvent

<SpecPhaseBadge label="setup-only" />

`watch()` 是 setup-only registration API，只存在于 borrowed 与 observed view。它用于观察状态变化，不是响应式渲染系统。

watch callback 接收 `StateEvent`：

```ts
state.watch((run, event) => {
  if (event.type === 'next') {
    // event.prev
    // event.next
    // event.reason
  }
});
```

当 state value 真的从 `prev` 变成 `next` 时，watcher 会收到：

```ts
{ type: 'next', prev, next, reason? }
```

如果 `Object.is(prev, next)` 为 true，这次写入不构成实际 state change。`setDefault()` 也不派发 `next` event，因为它只改变默认值计划，不代表 runtime value transition。

实例 unmount/dispose 造成 state slot 与 watcher 断开时，watcher 可以收到：

```ts
{ type: 'disconnect', reason: 'unmount' }
```

watch callback 本身不得隐式触发 render、commit 或 update。它只是记录和观察状态变化；是否更新 UI 仍然需要显式路径。

## Interaction State

Interaction state 是 State 的一个子领域，也可以理解成 State + Event 的复合模块。

```ts
const hovered = def.state.fromInteraction('hovered');
const disabled = def.state.fromInteraction('disabled');
```

声明 interaction state 会隐式引入 runtime-managed event wiring。原型作者不需要手写 `pointer.enter`、`pointer.leave`、`host:focus` 等 event subscription 来维护这些官方交互状态；runtime package、module、adapter 与 host capability 会协同完成这件事。

同一个交互主体上的同类 interaction state 永远是同一份 state reference。某个 asHook 内部如果调用 `fromInteraction('pressed')`，调用这个 asHook 的原型作者之后也可以直接调用 `fromInteraction('pressed')` 拿到同一份状态，而不必由 asHook 显式返回。

当前 interaction state 采用 borrowed view。长期上，某些 interaction state 可能更接近 observed view；但在 adapter 与 module 还不能绝对保证真实交互状态的阶段，允许原型作者修正这类状态仍然是必要的逃生能力。

## Lifecycle

State view 绑定到组件实例生命周期。

实例存活期间，view API 按 phase rule 可用。`unmounted` callback 执行期间，实例仍被视为存活；符合 runtime phase 的 state 操作仍然允许。

实例 dispose 后，所有 state view API 与 watch registration 都必须失效。内部 subscription 也必须清理，避免 dispose 之后仍然调用旧 watcher。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

State 的测试映射分为本域和 interaction 子领域：

| 测试实体                   | 主要覆盖                                                          |
| -------------------------- | ----------------------------------------------------------------- |
| `T-STATE-0001`             | definition、owned view phase guard、no-implicit-render            |
| `T-STATE-0002`             | owned、borrowed、observed、exposed state view capability          |
| `T-STATE-0003`             | finite kind、value spec metadata、JSON-compatible boundary        |
| `T-STATE-0004`             | watch setup-only、StateEvent、re-entrant order、lifecycle cleanup |
| `T-STATE-INTERACTION-0001` | interaction event wiring、callback order、shared subject identity |

这些测试把 state 从“内部变量”收敛为一组可执行的状态机边界：什么时候能定义、什么时候能读写、谁有能力写、谁只能观察，以及变化记录如何派发。

## 与其他规范的关系

- `Core` 定义 setup/runtime、JSON value boundary 与 channel model；State 明确不属于信息通路。
- `Props` 表达 App Maker 到 Component 的配置输入；State 表达 Component 自身的内部连续性。
- `Event` 表达 User 到 Component 的交互输入；interaction state 会使用 runtime-managed Event wiring 来维护官方交互状态。
- `Feedback` 把组件状态和结果传达给 User；State 自身不负责可感知呈现。
- `Rule` 可以读取 state 并执行 feedback intent，但 rule 是独立 API，不是 State 的一部分。
- `Lifecycle` 定义 state view 在 setup、runtime callback、unmounted 与 dispose 之间的可用边界。
